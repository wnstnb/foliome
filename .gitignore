# Secrets
.env
.env.keys
credentials.json
gmail-token.json
*-tokens.json
.gstack/

# Data (SQLite, sync output, chrome profiles, downloads, screenshots)
data/

# Config (populated from config-templates/ on setup — personal data after customization)
config/

# Claude Code local settings and runtime artifacts
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Dependencies
node_modules/

# Dashboard build output
dashboard/dist/
dashboard/node_modules/

# Debug artifacts
*.png
!docs/*.png
!assets/*.png

eng.traineddata